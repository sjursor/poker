{
  "rules": {
    "players": {
      "$uid": {
        ".read":  "auth != false",//"$uid === auth.uid",
        ".write": "auth != false"
      }
    },
    "rooms":{
      ".read": "auth != false",
      ".write":"auth != false"
    }
  }
}